@IsTest
public with sharing class VisualConfigurationControllerTest {
    
    @IsTest	
    static void parseVisualConfigurationTest(){
        VisualConfigurationController configObject = new VisualConfigurationController();
    }
}